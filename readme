First real project using MongoDB and Morphia.

I finally took time to play around with MongoDB and this is my first project. At today (19/02/2014), this is just the skeleton of a sample application which uses MongoDB as databases to store property configurations for entity called 'Cobrand'.
